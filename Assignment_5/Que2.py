b = bytes([65, 66, 67])
print(b) #b 'ABC'

# bytes is one of the data types in Python categorised under Binary data types.
# bytes are immutable sequence of bytes and ranges from 0 to 255
# In every programming language, the bytes ranes from 0 to 255 has 
# unique hex value internally and due to that reason the number declared 
# as 65, 66, 67 got converted into hex values associted with them respectively.